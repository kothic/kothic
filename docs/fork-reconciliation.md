# Fork reconciliation status

Short version: one modern Kothic can now reproduce the current Organic Maps,
CoMaps, and latest MAPS.ME fork outputs.  The checked-in MAPS.ME drules in the
old `omim` checkout are older than the latest MAPS.ME Kothic fork, so they are a
separate historical baseline.

## What We Have

| Consumer | Status | Mode to use |
| --- | --- | --- |
| Original Kothic | Kept as the base project and ported to Python 3. | Default renderer / MVT code paths. |
| Organic Maps | Reproduces the fork output byte-for-byte for all six generated styles. | `--priority-mode priority_files --runtime-condition-mode organicmaps` |
| CoMaps | Reproduces the fork output byte-for-byte for all six generated styles. | `--priority-mode priority_files --runtime-condition-mode comaps` |
| MAPS.ME latest fork | Reproduces the latest `mapsme/kothic` fork `.bin` output for all found styles. | `--priority-mode mapsme --runtime-condition-mode mapsme` |
| MAPS.ME checked-in `omim` drules | Does not match latest `mapsme/kothic`; this is expected. | Needs a future `mapsme-2016` mode if exact reproduction is required. |

## The Main Gotcha

There are two different MAPS.ME things:

1. **Latest MAPS.ME Kothic fork**:
   `mapsme/kothic` at `cbaff545dd5d2343dcbd30285884dd4afa509d93` from 2020.

2. **Old MAPS.ME drules checked into `omim`**:
   `omim/data/drules_proto*.bin`, generated around 2016 from submodule
   `tools/kothic` at `c32d9c5e22d6f42c582c2234e9881cda5f6db4f5`.

Those are not the same baseline.  The new `mapsme` mode targets the latest
`mapsme/kothic` fork, not the old checked-in `omim` binaries.

That is why this is true:

- new Kothic vs latest `mapsme/kothic` fork: **matches**;
- new Kothic vs checked-in `omim` drules: **does not match**.

This is not necessarily a bug in the new code.  It means the checked-in drules
were generated by an older compiler state.

## Why Checked-in MAPS.ME Drules Differ

Evidence from the local `omim` checkout:

- checked-in drules last changed at `efbce784d7` (`Ran the script`,
  2016-06-03);
- `omim` records `tools/kothic` submodule `c32d9c5` from 2016;
- the local `mapsme/kothic` fork checkout is `cbaff54` from 2020;
- the old submodule is not initialized in this checkout;
- current `mapcss-mapping.csv` contains duplicate active type
  `tourism|information|office`, which the old unpatched MAPS.ME compiler
  rejects.

The first visible protobuf difference is already behavioral, not just text
formatting: on `aeroway-aerodrome`, checked-in `omim` drules put the normal
caption in `primary` and `int_name` in `secondary`, while latest fork behavior
puts them the other way around for that style order.

So the old checked-in files should be treated as:

> "2016 MAPS.ME generated artefacts"

not as:

> "the expected output of latest `mapsme/kothic`".

## What Was Fixed Anyway

MAPS.ME's old `tools/unix/generate_drules.sh` does not pass `-t`.

Historical MAPS.ME Kothic defaulted to max zoom `19`; the modernized shared CLI
had defaulted to `20`.  That would create an extra zoom level if somebody used
new Kothic as a drop-in replacement for old MAPS.ME scripts.

So direct `libkomwm.py` now resolves default max zoom like this:

- `--priority-mode mapsme` and no `-t`: use `19`;
- priority-file mode and no `-t`: use `20`;
- explicit `-t`: always respect the explicit value.

## Fork Differences We Keep

### Organic Maps

Organic Maps uses priority files.  Important behavior:

- six generated styles;
- max zoom `20`;
- runtime-condition variants are deduplicated;
- runtime filtering is strict exact-match;
- priorities come from `*.prio.txt`;
- `casing-width-add` support stays here.

Verified output: all six `.bin` and `.txt` files matched the Organic Maps fork.

### CoMaps

CoMaps is close to Organic Maps, but keeps an extra unfiltered fallback runtime
variant.

Important behavior:

- six generated styles;
- max zoom `20`;
- runtime-condition variants are deduplicated;
- one unfiltered fallback variant is added;
- priority-file filtering remains strict.

Verified output: all six `.bin` and `.txt` files matched the CoMaps fork.

### MAPS.ME Latest Fork

MAPS.ME does not use priority files.  It uses old arithmetic priority logic.

Important behavior kept in `--priority-mode mapsme`:

- max zoom default `19`;
- old z-index clamp behavior;
- chooser matching against all classificator tags;
- raw runtime-condition variants;
- subset runtime-condition filtering;
- old arithmetic priorities for lines, casing, icons, captions, circles, and
  areas;
- old casing behavior for area styles when the class also has lines;
- old protobuf serialization quirks such as empty `dashdot {}` for casing
  lines.

Verified output: all found MAPS.ME style `.bin` files matched the latest
`mapsme/kothic` fork oracle:

- `clear/style-clear/style.mapcss`;
- `clear/style-night/style.mapcss`;
- `legacy/style-dark/style.mapcss`;
- `legacy/style-light/style.mapcss`.

## What To Do Next

If the goal is to support current consumers:

1. Keep `organicmaps`, `comaps`, and latest `mapsme` modes as they are.
2. Continue adding tests around exact fork output.
3. Do not change latest `mapsme` mode just to match old checked-in `omim`
   binaries.

If the goal is to reproduce old checked-in MAPS.ME `omim` drules exactly:

1. Add a separate mode, probably `--priority-mode mapsme-2016` or
   `--mapsme-baseline omim-2016`.
2. Base it on submodule commit `c32d9c5`, not on latest `mapsme/kothic`.
3. Patch or emulate the old duplicate-type behavior for current
   `mapcss-mapping.csv`.
4. Then compare against `omim/data/drules_proto*.bin`.

## Quick Verification Commands

Cheap checks:

```sh
python3 -m unittest discover -s tests
ruff check src tests integration-tests/full_drules_gen.py --exclude=src/drules_struct_pb2.py --target-version=py39
python3 -m compileall -q src tests integration-tests
git diff --check
```

Fork output checks:

- Organic Maps: run `integration-tests/full_drules_gen.py` with
  `--runtime-condition-mode organicmaps` and compare all six `.bin/.txt` files.
- CoMaps: run the same script with `--runtime-condition-mode comaps`.
- MAPS.ME latest: run `src/libkomwm.py` with
  `--priority-mode mapsme --runtime-condition-mode mapsme -t 19` and compare
  binary protobuf output for every MAPS.ME style file.

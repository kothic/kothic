# The core is using protobuf 3.3.0 still (3party/protobuf/), so no point to require a narrow newer version.
# Ubuntu 24.04's 3.21.12 and current protobuf 6.x both work with the checked-in generated bindings.
protobuf>=3.20.0,<7.0.0
